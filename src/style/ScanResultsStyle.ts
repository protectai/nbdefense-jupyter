import { stylesheet } from 'typestyle';

export const scanResultsStylesheet = stylesheet({
  header: {
    marginBottom: 0
  },
  list: {
    marginBlockStart: 0,
    marginBlockEnd: 0,
    paddingInlineStart: 0
  }
});
